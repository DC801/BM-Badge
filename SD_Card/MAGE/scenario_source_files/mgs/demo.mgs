include "header.mgs";

$nano = 0;
$vim = 1;
$emacs = 2;

on_load_demo {
	// testing
	player x = 10;
	player y = 10;
	follow_player_stack_init()
	// normal
	goto show_dialog_timewarp;
}
show_dialog_timewarp {
	command rtfm = command_rtfm;
	if (!demo_cutseen_timewarp) {
		camera fade out -> #000000 over 1ms;
		wait 200ms;
		player_control = off;
		player direction = south;
		show dialog {
			name "" "...."
			"Am I asleep?"
		};
		camera fade in -> #000000 over 2000ms;
		show dialog {
			PLAYER alignment BL
			"Huh? Is this the village?"
			"No, this is the village of the past. But\nI still have Ring Zero...."
			"This has got to be a dream."
		};
		player_control = on;
		demo_cutseen_timewarp = true;
	}
	hex_control = on;
}

show_dialog_bob_first_demo_map {
	if (!demo_bob_backstory) {
		show dialog {
			SELF "Name's Bob."
			"Stone Cold Bob Austin."
			"Right, what's yours, then?"
			PLAYER "You don't know who I am? We've both lived here for years!"
			SELF "Don't know nobody. All I know is, gotta keep everyone out."
			"You know, unless their name is Bob."
			PLAYER "Bob?"
			SELF "Bob. Camel case.\nNice and plain.\nTraditional."
			"'Cause this club is Bobs-only. You know, the Bobs-Only Club."
			PLAYER "Why is it Bobs only?"
			SELF "Why?\nWhy is it Bobs only?"
			"Why, it wouldn't be the Bobs-Only Club if I just let anyone in!"
		};
		demo_bob_backstory = true;
	} else if (demo_bob_notimplemented) {
		show dialog {
			SELF "So, what's your name, then?"
			PLAYER "The door doesn't work! What does it matter what my name is?"
			SELF "Not my job. My only job is to keep everyone out who's not named Bob."
		};
	} else {
		show dialog {
			SELF "So, what's your name then?"
		};
		if (player name != Bob) {
			show dialog {
				PLAYER "Um.... it's not Bob."
				SELF "Sorry, can't unlock the door for ya. Bobs only."
			};
			return; // return early
		}
		show dialog {
			PLAYER "Why, what luck!\nMy name happens to be Bob!"
			SELF "Nice. Great name.\nI'll unlock the door."
		};
		entity Bob position -> entity_path origin over 666ms;
		entity Bob direction = west;
		wait 666ms;
		entity Bob direction = south;
		show dialog {
			SELF "There you go. It's open."
		};
		player position -> geometry predoor origin over 666ms;
		player position -> geometry door origin over 1333ms;
		wait 666ms;
		player direction = east;
		show dialog {
			PLAYER "Hey, what gives?\nI still can't get in."
		};
		entity Bob direction = west;
		show dialog {
			SELF "Oh, it's unlocked."
			PLAYER "Then why won't it open?"
			SELF "Oh. The door's not implemented yet."
			PLAYER "...."
			"You could've said that from the start!"
			SELF "Not my job. My only job is to keep everyone out who's not named Bob."
		};
		entity Bob direction = south;
		demo_bob_notimplemented = true;
	}
}

show_dialog_sheep_demo_map {
	show dialog { SELF "...." };
}
show_dialog_dsheep_demo_map {
	show dialog { SELF "...?" };
}

show_dialog_max_demo_map {
	face_player()
	show dialog {
		SELF "Ah, a citizen!"
		"I wondered if you could help me!"
		PLAYER "What do you need?"
		SELF "My name's Swagger! Max Swagger!"
		"I was doing a bit of reconnoissance, looking for a place where I can open my new fashion shop."
		"The board here says there's space available, but I can't seem to find the building it's talking about."
		"The only thing actually here is the Bobs-Only Club!"
		PLAYER "Well, the town isn't fully implemented yet, so... you might have to wait a few more weeks for the rest of the buildings to return."
	};
	player_control = off;
	self direction -= 2;
	wait 800ms;
	self direction += 1;
	wait 800ms;
	player_control = on;
	face_player()
	show dialog {
		SELF "Not fully implemented?"
		PLAYER "Sorry."
		"But I can personally guarantee you can have a building all to yourself when the time comes."
		SELF "Oh! That's good news! All to myself?"
		"I'm picturing two stories... no, three!"
		"Lush woven carpets... vaulted ceilings... beautiful oak doors... hand-carved decorative moulding...."
		PLAYER "Um, well.... You'll definitely get a building."
		SELF "Excellent! Then I shall return when the time comes!"
		"Until that glorious day, farewell!"
	};
	self position -> entity_path length over 9000ms;
	self position = geometry hiding;
}

show_dialog_timmy_demo_map {
	interrupt_walk()
	show dialog {
		SELF "I'm practicing for my triathlon!"
		"Running is my weakest event, so I'm working hard to improve my time!"
		PLAYER "Don't you need legs to run?"
		SELF "...?"
		"No?"
	};
	resume_walk()
}

show_dialog_kid_demo_map {
	interrupt_walk()
	show dialog {
		SELF "I'm gonna be a Blitzball when I grow up!"
	};
	resume_walk()
}

show_dialog_goose_demo_map {
	show dialog {
		SELF "Honk!"
	};
}

show_dialog_beatrice_demo_map {
	player_control = off;
	face_player()
	show dialog {
		SELF "Back when he was a young man, my husband Delmar hand-carved all the statues on the fountains around town."
		"Such a shame that this one isn't working. I wonder if something is clogging the pump."
	};
	self direction += 1;
	wait 600ms;
	self direction -= 2;
	wait 600ms;
	face_player()
	show dialog {
		SELF "Between you and me.... I think the goose has something to do with it."
	};
	player_control = on;
	self direction = south;
}

show_dialog_trekkie_demo_map {
	face_player()
	show dialog {
		SELF "Me heard you will soon have a birthday!"
		PLAYER "That's right! I'll be turning 16, and I'll get to be an official village mage!"
		SELF "Me hope you have happy birthday!"
		PLAYER "Thanks very much, %Trekkie%!"
	};
}

show_dialog_verthandi_demo_map {
	face_player()
	show dialog {
		SELF "I'm so glad the goats made up and are friends again!"
	};
	self direction = south;
}

show_dialog_goat_demo_map {
	show dialog { SELF "Baaahhh!" };
}

show_dialog_cleo1_demo_map {
	show dialog {
		Cat "Meowrrow."
		Cleo "Oh, you don't say!"
		Cat "Meow!"
		Cleo "He didn't!"
	};
	self on_interact = show_dialog_cleo2_demo_map;
}

show_dialog_cleo2_demo_map {
	show dialog {
		Cat "Meowwowow."
		Cleo "That's what I've been saying this whole time!"
		Cat "Meowwrrrr!"
		Cleo "Oh, I know!"
	};
	self on_interact = show_dialog_cleo1_demo_map;
}

show_dialog_cat_demo_map {
	show dialog {
		PLAYER "Hey there, cat!"
		Cat "...."
		Cleo "Excuse me, but we were in the middle of a conversation."
		PLAYER "Oh, sorry. Carry on."
	};
}

show_dialog_smith_demo_map {
	face_player()
	show dialog {
		SELF "I came here on a blacksmithing scholarship, but there's not much blacksmithing to do at the moment."
		"I think I'll stick around anyway. See what happens."
	};
}

check_if_player_is_goat_high_demo_map {
	if (player intersects geometry high) {
		entity goat1 on_tick = {
			entity goat1 position -> geometry low1 origin over 600ms;
			entity goat1 position -> geometry low1 length over 2000ms forever;
		};
		entity goat2 on_tick = {
			entity goat2 position -> geometry low2 origin over 733ms;
			entity goat2 position -> geometry low2 length over 2266ms forever;
		};
		goto check_if_player_is_goat_low_demo_map;
	}
}

check_if_player_is_goat_low_demo_map {
	if (player intersects geometry low) {
		entity goat1 on_tick = {
			entity goat1 position -> geometry high1 origin over 600ms;
			entity goat1 position -> geometry high1 length over 2000ms forever;
		};
		entity goat2 on_tick = {
			entity goat2 position -> geometry high2 origin over 733ms;
			entity goat2 position -> geometry high2 length over 2266ms forever;
		};
		goto check_if_player_is_goat_high_demo_map;
	}
}

loop_on_path_30s_demo_map {
	self position -> entity_path length over 20000ms forever;
}

loop_on_path_10s_demo_map {
	self position -> entity_path length over 6666ms forever;
}

loop_on_path_3s_demo_map {
	self position -> entity_path length over 2000ms forever;
}

// Chaos fish
show_dialog_demo_end_dream_q {
	show dialog {
		SELF
		portrait goldfish
		"End this dream?"
		> "Yes (save and quit)" = {
			save slot;
			load map main_menu;
		}
		> "Yes (quit without saving)" = {
			load map main_menu;
		}
		> "No (continue dream)" = {
			self on_interact = show_dialog_demo_end_dream_q;
		}
	};
}

// Unused (from original prerelease demo; did not get plugged in properly)
 dialog dialog_max3_demo_map {
	PLAYER "Not much of a market for fashion here, I would've thought."
	"Best of luck, Max Swagger!"
}

// --------------------------------------------------- FOLLOWING ENTITIES

$follow_delay = 2;
$queue_diff = 6;
$queue_size = 10;

// follow_player_stack_init { // ARRAY VERSION
// 	offset_self_x = self x;
// 	if (curr_follower == $nano) { offset_self_x -= 8; }
// 	if (curr_follower == $emacs) { offset_self_x += 8; }
// 	offset_self_y = self y + 1;
// 	step = get_diff(player_x, offset_self_x) / 5;
// 	player_x = player x;
// 	player_y = player y;
// 	if (player x > offset_self_x) {
// 		new Array followingPlayerX = [
// 			offset_self_x,
// 			offset_self_x + step,
// 			offset_self_x + step * 2,
// 			offset_self_x + step * 3,
// 			offset_self_x + step * 4,
// 			player x,
// 			player x,
// 			player x,
// 			player x,
// 			player x,
// 		]
// 	} else {
// 		new Array followingPlayerX = [
// 			offset_self_x,
// 			player x + step * 4,
// 			player x + step * 3,
// 			player x + step * 2,
// 			player x + step,
// 			player x,
// 			player x,
// 			player x,
// 			player x,
// 			player x,
// 		]
// 	}
// 	step = get_diff(player_y, offset_self_y) / 5;
// 	if (player y > offset_self_y) {
// 		new Array followingPlayerY = [
// 			offset_self_y,
// 			offset_self_y + step,
// 			offset_self_y + step * 2,
// 			offset_self_y + step * 3,
// 			offset_self_y + step * 4,
// 			player y,
// 			player y,
// 			player y,
// 			player y,
// 			player y,
// 		]
// 	} else {
// 		new Array followingPlayerY = [
// 			offset_self_y,
// 			player y + step * 4,
// 			player y + step * 3,
// 			player y + step * 2,
// 			player y + step,
// 			player y,
// 			player y,
// 			player y,
// 			player y,
// 			player y,
// 		]
// 	}
// 	curr_self_queue = 0;
// 	curr_player_queue = 6;
// }
follow_player_stack_init {
	offset_self_x = self x;
	if (curr_follower == $nano) { offset_self_x -= 8; }
	if (curr_follower == $emacs) { offset_self_x += 8; }
	offset_self_y = self y + 1;
	// first in xy queue is follower's curr location
	queue_x0 = offset_self_x *1;
	queue_y0 = offset_self_y *1;
	// next four pos in queue are equal-sized steps between follower and followee
	player_x = player x;
	player_y = player y;
	// debug!("PLAYER ($player_x$, $player_y$), follower ($offset_self_x$, $offset_self_y$)")
	diff = get_diff(player_x, offset_self_x);
	step1 = diff / 5;
	step2 = 2 * diff / 5;
	step3 = 3 * diff / 5;
	step4 = 4 * diff / 5;
	// debug!("X diff: $diff$, steps: [$step1$, $step2$, $step3$, $step4$]")
	if (player x > offset_self_x) {
		queue_x1 = offset_self_x + step1;
		queue_x2 = offset_self_x + step2;
		queue_x3 = offset_self_x + step3;
		queue_x4 = offset_self_x + step4;
	} else {
		queue_x1 = player x + step4;
		queue_x2 = player x + step3;
		queue_x3 = player x + step2;
		queue_x4 = player x + step1;
	}
	diff = get_diff(player_y, offset_self_y);
	step1 = diff / 5;
	step2 = 2 * diff / 5;
	step3 = 3 * diff / 5;
	step4 = 4 * diff / 5;
	// debug!("Y diff: $diff$, steps: [$step1$, $step2$, $step3$, $step4$]")
	if (player y > offset_self_y) {
		queue_y1 = offset_self_y + step1;
		queue_y2 = offset_self_y + step2;
		queue_y3 = offset_self_y + step3;
		queue_y4 = offset_self_y + step4;
	} else {
		queue_y1 = player y + step4;
		queue_y2 = player y + step3;
		queue_y3 = player y + step2;
		queue_y4 = player y + step1;
	}
	// the rest of the queue is the followee's current pos
	// (the player will fill these in as they get to them)
	queue_x5 = player x;
	queue_x6 = player x;
	queue_x7 = player x;
	queue_x8 = player x;
	queue_x9 = player x;
	queue_y5 = player y;
	queue_y6 = player y;
	queue_y7 = player y;
	queue_y8 = player y;
	queue_y9 = player y;
	curr_self_queue = 0;
	curr_player_queue = 6;
}

fn save_player_pos_if ($n, $xVar, $yVar) {
	if (curr_player_queue == $n) {
		$xVar = player x;
		$yVar = player y;
	}
}
// save_player_pos { // ARRAY VERSION
// 	followingPlayerX[curr_player_queue] = player x;
// 	followingPlayerY[curr_player_queue] = player y;
// }
save_player_pos {
	save_player_pos_if(0, queue_x0, queue_y0)
	save_player_pos_if(1, queue_x1, queue_y1)
	save_player_pos_if(2, queue_x2, queue_y2)
	save_player_pos_if(3, queue_x3, queue_y3)
	save_player_pos_if(4, queue_x4, queue_y4)
	save_player_pos_if(5, queue_x5, queue_y5)
	save_player_pos_if(6, queue_x6, queue_y6)
	save_player_pos_if(7, queue_x7, queue_y7)
	save_player_pos_if(8, queue_x8, queue_y8)
	save_player_pos_if(9, queue_x9, queue_y9)
}
fn load_self_pos_if ($n, $xVar, $yVar) {
	if (curr_self_queue == $n) {
		self x = $xVar;
		self y = $yVar;
		// self_x = self x;
		// self_y = self y;
		// capture_entity_direction("%SELF%", dir)
		// if (dir == 0) {
		// 	debug!("Queue $curr_self_queue$: $self_x$, $self_y$ (<m>NORTH</>)")
		// } else if (dir == 1) {
		// 	debug!("Queue $curr_self_queue$: $self_x$, $self_y$ (<y>east</>)")
		// } else if (dir == 2) {
		// 	debug!("Queue $curr_self_queue$: $self_x$, $self_y$ (<c>SOUTH</>)")
		// } else if (dir == 3) {
		// 	debug!("Queue $curr_self_queue$: $self_x$, $self_y$ (<r>west</>)")
		// }
	}
}
// load_self_pos { // ARRAY VERSION
// 	self x = followingPlayerX[curr_self_queue];
// 	self y = followingPlayerY[curr_self_queue];
//	if (curr_follower == $nano) { self x += 8; }
//	if (curr_follower == $emacs) { self x -= 8; }
//	self y -= 1;
// }
load_self_pos {
	load_self_pos_if(0, queue_x0, queue_y0)
	load_self_pos_if(1, queue_x1, queue_y1)
	load_self_pos_if(2, queue_x2, queue_y2)
	load_self_pos_if(3, queue_x3, queue_y3)
	load_self_pos_if(4, queue_x4, queue_y4)
	load_self_pos_if(5, queue_x5, queue_y5)
	load_self_pos_if(6, queue_x6, queue_y6)
	load_self_pos_if(7, queue_x7, queue_y7)
	load_self_pos_if(8, queue_x8, queue_y8)
	load_self_pos_if(9, queue_x9, queue_y9)
	if (curr_follower == $nano) { self x += 8; }
	if (curr_follower == $emacs) { self x -= 8; }
	self y -= 1;
}
follow_player {
	// wait 20ms;
	if (player current_animation != 1) {
		if (should_follow_player) {
			should_follow_player = false;
			follow_delay_tick = $follow_delay;
		} else if (follow_delay_tick != 0) {
			follow_delay_tick -= 1;
		} else if (am_following_player) {
			am_following_player = false;
			self current_animation = 0;
		}
	} else {
		save_player_pos()
		curr_player_queue = (curr_player_queue + 1) % $queue_size;
		if (!should_follow_player) {
			should_follow_player = true;
			follow_player_stack_init()
			follow_delay_tick = $follow_delay;
		} else if (follow_delay_tick != 0) {
			follow_delay_tick -= 1;
		} else if (!am_following_player) {
			am_following_player = true;
			self current_animation = 1;
		}
	}

	// MOVE FOLLOWER
	if (am_following_player) {
		my_old_x = self x;
		my_old_y = self y;
		// advance the queue
		curr_self_queue = (curr_self_queue + 1) % $queue_size;
		// set position based on the new queue value
		load_self_pos()
		// get the absolute value of the delta
		if (self x > my_old_x) {
			my_diff_x = self x - my_old_x;
		} else {
			my_diff_x = my_old_x - self x;
		}
		if (self y > my_old_y) {
			my_diff_y = self y - my_old_y;
		} else {
			my_diff_y = my_old_y - self y;
		}
		// turn the entity NSEW - check which axis changed more
		if (my_diff_x > my_diff_y) {
			if (self x > my_old_x) {
				self direction = east;
			} else if (self x < my_old_x) {
				self direction = west;
			}
		} else {
			if (self y > my_old_y) {
				self direction = south;
			} else if (self y < my_old_y) {
				self direction = north;
			}
			// if position is the same, don't turn at all (use current direction)
		}
	}
}


interact_demo_nano {
	if (curr_follower != $nano) {
		if (curr_follower == $vim) {
			show dialog vim_quit { Vim ":wq" };
			entity Vim on_tick = face_player;
			entity Vim current_animation = 0;
		} else if (curr_follower == $emacs) {
			show dialog emacs_quit { Emacs "You'll be back." };
			entity Emacs on_tick = face_player;
			entity Emacs current_animation = 0;
		}
		curr_follower = $nano;
		follow_player_stack_init()
		entity Nano on_tick = follow_player;
		show dialog { SELF "I'll help now!" };
		
	} else {
		face_player()
		show dialog { SELF "I like turtles." };
	}
}

interact_demo_vim {
	if (curr_follower != $vim) {
		if (curr_follower == $nano) {
			show dialog nano_quit { Nano "Writing out!" };
			entity Nano on_tick = face_player;
			entity Nano current_animation = 0;
		} else if (curr_follower == $emacs) {
			show dialog emacs_quit;
			entity Emacs on_tick = face_player;
			entity Emacs current_animation = 0;
		}
		curr_follower = $vim;
		follow_player_stack_init()
		entity Vim on_tick = follow_player;
		show dialog { SELF ":e follow" };
	} else {
		face_player()
		show dialog { SELF ":up" };
	}
}

interact_demo_emacs {
	if (curr_follower != $emacs) {
		if (curr_follower == $nano) {
			show dialog nano_quit;
			entity Nano on_tick = face_player;
			entity Nano current_animation = 0;
		} else if (curr_follower == $vim) {
			show dialog vim_quit;
			entity Vim on_tick = face_player;
			entity Vim current_animation = 0;
		}
		curr_follower = $emacs;
		follow_player_stack_init()
		entity Emacs on_tick = follow_player;
		show dialog { SELF "About time." };
	} else {
		face_player()
		show dialog { SELF "'Sup?" };
	}
}